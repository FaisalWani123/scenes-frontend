import { FindPackageByIdRequest } from './find-package-by-id-request';

describe('FindPackageByIdRequest', () => {
  it('should create an instance', () => {
    expect(new FindPackageByIdRequest()).toBeTruthy();
  });
});
