import { FindAllPackagesRequest } from './find-all-packages-request';

describe('FindAllPackagesRequest', () => {
  it('should create an instance', () => {
    expect(new FindAllPackagesRequest()).toBeTruthy();
  });
});
