import { FindPackageByIdResponse } from './find-package-by-id-response';

describe('FindPackageByIdResponse', () => {
  it('should create an instance', () => {
    expect(new FindPackageByIdResponse()).toBeTruthy();
  });
});
