import { FindAllPackagesResponse } from './find-all-packages-response';

describe('FindAllPackagesResponse', () => {
  it('should create an instance', () => {
    expect(new FindAllPackagesResponse()).toBeTruthy();
  });
});
