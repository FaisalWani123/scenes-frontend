import { FriendRequestManager } from './friend-request-manager';

describe('FriendRequestManager', () => {
  it('should create an instance', () => {
    expect(new FriendRequestManager()).toBeTruthy();
  });
});
