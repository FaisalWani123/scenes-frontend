import { FriendListResponse } from './friend-list-response';

describe('FriendListResponse', () => {
  it('should create an instance', () => {
    expect(new FriendListResponse()).toBeTruthy();
  });
});
