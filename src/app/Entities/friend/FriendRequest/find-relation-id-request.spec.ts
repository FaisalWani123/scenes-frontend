import { FindRelationIdRequest } from './find-relation-id-request';

describe('FindRelationIdRequest', () => {
  it('should create an instance', () => {
    expect(new FindRelationIdRequest()).toBeTruthy();
  });
});
