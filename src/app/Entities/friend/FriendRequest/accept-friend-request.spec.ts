import { AcceptFriendRequest } from './accept-friend-request';

describe('AcceptFriendRequest', () => {
  it('should create an instance', () => {
    expect(new AcceptFriendRequest()).toBeTruthy();
  });
});
